function addStatus(reviewHTML) {
  const status = document.getElementById('booking_status');
}

function refreshbookings(innerHTML) {
  const newbookingstatus = document.getElementById('new-booking-status');
  newbookingstatus.innerHTML = innerHTML;
}
